/**
 * Entorno de producción (build por defecto de `ng build` / `pnpm build`).
 *
 * PLACEHOLDER — ver la nota completa en apps/portal-cliente/src/environments
 * /environment.ts (mismo patrón). Reemplazar `apiUrl` antes de cualquier
 * deploy real.
 */
export const environment = {
  production: true,
  apiUrl: 'https://REEMPLAZAR-EN-VERCEL-toolboxjl-api.up.railway.app/api/v1',
};
