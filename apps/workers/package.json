{
  "name": "@toolboxjl/workers",
  "version": "0.0.0",
  "private": true,
  "description": "ToolBox JL — jobs batch standalone (Railway, cron externo). Sprint 5: MoraCalculatorJob (RF-4.3, HU-5.3). NO comparte NestJS con apps/api — cada job es un script de una sola pasada que corre y termina, sin servidor persistente.",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "jest",
    "mora:calcular": "ts-node -T src/main.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.2.1"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^24.13.3",
    "jest": "^29.7.0",
    "ts-jest": "^29.4.12",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
